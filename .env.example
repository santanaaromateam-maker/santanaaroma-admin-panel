# =============================================================================
# Santana Aroma — Admin panel (React / Vite)
# Copy to .env and fill in your values
# =============================================================================

# Base URL of the FastAPI backend (no trailing slash)
VITE_API_URL=http://localhost:8001

# Production (Render) — debe ser santanaromateam-api (con "team"), NO santanaroma-api:
# VITE_API_URL=https://santanaromateam-api.onrender.com
